[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F"]
ignore = [
    "E501",  # line too long — Chinese comments routinely exceed 88 chars
    "E402",  # module level import not at top — main.py uses conditional imports
]
exclude = [".venv"]
